<Window x:Class="WeatherApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WeatherApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="300" Width="415" ResizeMode="NoResize">
    <Window.Resources>
        <Style TargetType="{x:Type TextBox}" x:Key="RoundedTextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBoxBase}">
                        <Border x:Name="border"
                                CornerRadius="10" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Background="{TemplateBinding Background}"
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Focusable="false"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter TargetName="border" Property="Opacity" Value="0.56"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="true">
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="{x:Type Button}" x:Key="RoundedButton">
            <Setter Property="Background" Value="#FFDDDDDD"/>
            <Setter Property="BorderBrush" Value="#FF707070"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border"
                                CornerRadius="10" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Background="{TemplateBinding Background}"
                                SnapsToDevicePixels="true">
                            <ContentPresenter x:Name="contentPresenter"
                                              Focusable="False"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              Margin="{TemplateBinding Padding}"
                                              RecognizesAccessKey="True"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsDefaulted" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="border" Property="Background" Value="#FFBEE6FD"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF3C7FB1"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter TargetName="border" Property="Background" Value="#FFC4E5F6"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF2C628B"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter TargetName="border" Property="Background" Value="#FFF4F4F4"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#FFADB2B5"/>
                                <Setter Property="Foreground" Value="#FF838383"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Background="#FFBBD29B">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <TextBox x:Name="Search_tb" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"
                 TextWrapping="Wrap"
                 VerticalAlignment="Center" Height="32"
                 Grid.ColumnSpan="3" Margin="20,0,52,0"
                 Style="{StaticResource RoundedTextBox}" BorderThickness="1,1,1,1"/>
        <Button x:Name="Search_btn" Grid.Column="2" Content="Искать" Margin="76,0,20,0"
                VerticalAlignment="Center" Grid.ColumnSpan="2" Height="32"
                Style="{StaticResource RoundedButton}" Click="OnRefreshClicked"/>
        <Grid Grid.ColumnSpan="4"
      Grid.Row="1"
      Grid.RowSpan="2"
      Margin="20,20,20,10"  Background="#FF9BD0B2"
      VerticalAlignment="Top">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" MinWidth="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Image x:Name="WeatherToday_img"
               Grid.Row="0"
               Grid.RowSpan="5"               
               HorizontalAlignment="Left"
               VerticalAlignment="Top"
               Width="100"  Height="100" Margin="34,19,0,0"   Source="images/placeholder.png" Grid.ColumnSpan="2" />
                <TextBlock x:Name="TemperatureToday_tb" Grid.Column="1" Grid.Row="0"
                   Text="Температура: --°C"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   FontSize="14" FontFamily="Segoe UI" FontWeight="SemiBold" Margin="5"/>
                <TextBlock x:Name="WindToday_tb" Grid.Column="1" Grid.Row="1"
                   Text="Ветер: -- м/с"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   FontSize="14" FontFamily="Segoe UI"
                   Margin="5"/>

                <TextBlock x:Name="FeelsLike_tb" Grid.Column="1" Grid.Row="2"
                   Text="Ощущается как: --°C"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   FontSize="14" FontFamily="Segoe UI"
                   Margin="5"/>

                <TextBlock x:Name="HumidityToday_tb" Grid.Column="1" Grid.Row="3"
                   Text="Влажность: --%"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   FontSize="14" FontFamily="Segoe UI"
                   Margin="5"/>

                <TextBlock x:Name="Pressure_tb" Grid.Column="1" Grid.Row="4"
                   Text="Давление: -- мм рт. ст."
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   FontSize="14" FontFamily="Segoe UI"
                   Margin="5"/>
            </Grid>
        </Grid>
        <ComboBox x:Name="day_cb" HorizontalAlignment="Left" Margin="20,79,0,0" VerticalAlignment="Top" SelectedIndex="0" Width="103" Height="21" Grid.RowSpan="2" SelectionChanged="OnDateOrTimeChanged" RenderTransformOrigin="-0.23,0.309" Grid.ColumnSpan="2"/>
        <ComboBox x:Name="time_cb" Grid.ColumnSpan="2" HorizontalAlignment="Center" Margin="0,79,0,0" VerticalAlignment="Top" SelectedIndex="0" Width="103" Height="21" Grid.RowSpan="2" SelectionChanged="OnDateOrTimeChanged" RenderTransformOrigin="0.917,0.621" Grid.Column="1"/>
    </Grid>
</Window>